# AI Limits 常见问题

[English](FAQ.md) | 简体中文

## “History”会在手动刷新还是自动刷新后更新？

两者都会。成功的 Connect、单个服务 Refresh、顶部刷新按钮或定时自动刷新，都会新增一条标准化类型化 History 观测，其中包含该服务本次返回的配额、计数或支出以及余额指标。History 只绘制配额指标；计数或支出和余额观测仍会保存，但不会绘图。

失败、延后、跳过、被更新请求取代或格式异常的刷新不会新增历史观测，也不会写入
零值或把上一次的数值复制到当前时间。

## 自动刷新需要保持侧边栏打开吗？

不需要。AI Limits 使用 Chrome 闹钟和 Manifest V3 后台 Service Worker，因此
关闭侧边栏不会停止定时刷新。重新打开侧边栏时，它会读取扩展本地存储中的最新
数据。

## 自动刷新需要满足哪些条件？

- **Settings** 中的 **Automatic refresh** 必须保持开启。
- 至少有一个服务保持连接；没有已连接服务时，AI Limits 会删除刷新闹钟。
- Chrome 仍然保留该服务的可选访问权限。
- 对于 ChatGPT、Claude、Kimi、Cursor 和 Grok，服务商的浏览器登录会话仍然可用；
  ElevenLabs 和 New API 则需要已保存且处于有效状态的 API 密钥；New API 还
  需要其精确实例权限。
- Chrome 正在运行，并且能够运行后台扩展。

闹钟大约每 15 分钟运行一次。Chrome 调度、设备休眠、服务商退避和网络状况都
可能延迟刷新，因此这不是严格精确的采样间隔。

## Chrome 关闭或电脑休眠时会怎样？

Chrome 完全关闭或设备休眠时，AI Limits 无法刷新。恢复运行后，Chrome 可能
补发已延迟的闹钟。扩展后台或 Chrome 启动时，AI Limits 也会检查并重建刷新
闹钟，但不会为错过的时间段补造历史观测。

## 手动刷新成功后，自动刷新也一定能成功吗？

对于 ChatGPT、Claude、Cursor 的基础用量以及 Grok，只要权限和浏览器会话仍然有效，通常可以；
对于 ElevenLabs 或 New API，只要保存的密钥和主机权限仍处于有效状态，通常也可以。但手动刷新可以
绕过定时退避，自动刷新则会遵守退避时间。Kimi 手动刷新还可能执行交互式会话
恢复。Cursor 手动刷新还可能通过已经打开的 Cursor 页面请求 Grok Bot 和额外
Credits JSON；如果当前没有打开的标签页，可能会短暂打开一个非活动用量页。自动
刷新绝不会打开标签页或向页面注入代码。定时刷新会保留上次有效的页面数值，
直到 Grok Bot 的每周重置，并说明为何未能刷新。

Kimi 还有下述额外限制。

## 为什么 Cursor 的 Grok Bot 或额外 Credits 可能只在手动刷新后出现？

Cursor 的基础月度和按量用量会在后台刷新。Grok Bot 和额外 Credits Dashboard
接口会拒绝跨源 POST（`Invalid origin for state-changing request`），因此 AI
Limits 只会在 Connect 或明确的手动 Refresh 期间尝试请求。它会优先使用已经
打开的 `cursor.com` 标签页；如果当前没有打开的标签页，可能会短暂创建一个
非活动用量页，最多等待 10 秒，并且只关闭它自己创建的标签页。扩展自带的函数
会验证精确来源，只发送三个固定的只读 Dashboard 请求，并把 JSON 返回扩展
上下文进行 Schema 验证。它不会直接检查页面渲染内容、浏览器存储或 Cookie
值；Chrome 仍会把已登录的 Cursor Cookie 附加到这些固定的同源请求。它不会
激活 Cursor 标签页，定时刷新也绝不会打开标签页或向 Cursor 页面注入代码。
上次有效的 Grok Bot 和额外 Credits 会保留在卡片上，直到 Grok Bot 的每周
重置；卡片会说明为何未能刷新。

## 为什么 Kimi 自动刷新不一定成功，而手动刷新可能打开后台标签页？

Kimi 自动刷新属于尽力而为，并不保证每次都能成功；手动 Connect 或 Refresh
可能会在后台短暂打开一个非活动 Kimi 标签页以恢复会话。

Kimi 定时刷新绝不会打开标签页。交互式 Connect 或 Refresh 最多打开一个非活动的临时 Kimi 标签页，最多等待 10 秒完成恢复，并且只关闭它自己创建的标签页。
它可以使用旧版 Kimi Cookie，或读取已经打开的 Kimi 页面中的 `access_token`。
如果两者都不可用，本次自动刷新会延后到之后的刷新，保留最后一次成功数据，
并且不会新增历史观测。

手动 **Connect** 或 **Refresh** 属于交互式操作，可能会使用这个非活动首页
标签页，让 Kimi 自己刷新会话。因此，Kimi 手动刷新可能成功，而定时刷新仍然
无法成功。

## 为什么 ElevenLabs 需要 API 密钥？登录后可以重新打开设置页面吗？

ElevenLabs 使用由用户创建的 API 密钥；验证成功后，AI Limits 会将其保存在本地，定时刷新不会打开 ElevenLabs 标签页。

点击 **Connect ElevenLabs** 会同时打开侧边栏内的指南和 ElevenLabs 官方 API
密钥页面。如果页面要求登录，请先完成登录，再用指南中的 **Open API keys page**
重新打开页面。创建名为 **AI Limits** 的密钥，选择 **User → Read**，并避免
生成或写入权限。ElevenLabs 并未正式公布该权限选项与订阅接口之间的精确对应
关系，因此 **Validate & connect** 会先去除密钥首尾空白，再验证真实的只读请求；
只有验证成功才会保存，AI Limits 也不会替你启用更宽泛的权限。

只有你主动开始设置或点击重新打开时，扩展才会打开普通 ElevenLabs 页面。连接
完成后，手动和定时用量刷新都在后台调用 API，不会打开或检查 ElevenLabs
标签页。

## ElevenLabs 密钥如何保存？密钥被拒绝后会怎样？

AI Limits 会将密钥单独保存在 chrome.storage.local，并通过 Chrome 存储访问级别限制为仅受信任的扩展上下文可读。

保存的密钥只会作为 xi-api-key 请求头发送到 https://api.elevenlabs.io，用于只读订阅请求。

该密钥不受操作系统钥匙串加密保护；能够访问未锁定 Chrome 配置文件、扩展 DevTools 或本地配置文件的人仍可能检查到它。

该密钥绝不会写入用量状态、History、截图、报告、日志、分析系统或开发者后端。

如果保存的 ElevenLabs 密钥被拒绝，AI Limits 会停止定时 ElevenLabs 请求，并保留过期的标准化用量和 History，直到替换或删除密钥。侧边栏会显示 **Replace key**；替换失败不会改动原有密钥。

断开 ElevenLabs、选择 Delete all local data、卸载扩展或清除扩展存储，都会删除保存的密钥。
从外部移除主机权限则会将 ElevenLabs 标记为需要权限，并保留本地
配置、保存的密钥、标准化用量、刷新状态和 History，直到重新连接或明确断开或
删除。

如果 Chrome 无法撤销最后一个所有者的主机权限，本地实例删除仍然有效，并会保留持久清理证据供之后重试。

## New API 支持哪些模式？应该输入什么网址？

AI Limits 支持多个相互独立的 New API 实例。每个连接分别拥有自己的标准化基础
网址、可选标签、Relay Key、当前用量、刷新状态、替换或拒绝状态和 History。
扩展只读取该密钥的 `/api/usage/token/` 响应：有上限的密钥显示总额、已用和
剩余配额；无限额密钥显示绝对用量，不会虚构百分比。扩展不读取账号钱包、订阅
或管理员数据。

位于同一来源的两个 New API 实例只会共享 Chrome 的浏览器全局来源权限；它们绝不会共享 Relay Key、标签、用量状态或 History。断开一个实例会在权限清理前
只删除该实例的密钥、配置、用量、刷新状态和 History。只要同一来源仍有其他
实例，授权就会保留；断开最后一个所有者时会尽力移除授权，并保留可供重试的
持久清理证据。若从外部移除授权，所有受影响实例会改为需要权限，同时保留非
机密配置、保存的密钥、标准化用量、刷新状态和 History，直到重新连接或明确
断开或删除。

可以粘贴站点首页、控制台网址、`/v1`、`/v1/messages` 或
`/api/usage/token/`。AI Limits 会移除这些已知后缀，保留部署子路径，并通过
`/api/status` 验证结果。除本地开发地址外，必须使用 HTTPS。

用量请求本身只读，但 Relay Key 不一定是“仅用量”凭据；同一个密钥仍可能
具备调用模型的能力。建议使用专用密钥，并按需在 New API 中配置配额、模型、
IP 等限制。验证成功的网址和密钥会保存在本地，用于后台刷新。完整边界请参阅
[支持的服务](SUPPORTED_PROVIDERS.zh-CN.md)。

## 自动刷新会打开服务商标签页吗？

不会。定时刷新是非交互式的，绝不会创建服务商标签页。只有交互式的 Kimi
**Connect** 或 **Refresh** 才可能为了恢复会话而短暂创建一个非活动 Kimi
标签页。交互式的 Cursor **Connect** 或 **Refresh** 在当前没有打开的
`cursor.com` 标签页时，可能会短暂创建一个非活动用量页，最多等待 10 秒，
并且只关闭它自己创建的标签页。ElevenLabs 只会在你主动开始设置或选择
**Open API keys page** 时打开普通 API 密钥页面。New API 的设置和刷新不会
打开服务商标签页。

## “History”保存什么？保存多久？

History 会按实例保存成功的标准化配额、计数或支出以及余额观测。以货币表示的
支出计数和余额属于标准化用量数据，并不是原始支付交易历史。History 只绘制配额
指标；计数或支出和余额观测仍会保存，但不会绘图。History 绝不会保存凭据、
服务商原始响应，也不会重建服务商过去的历史。失败、延后、跳过或格式异常的
刷新不会新增观测；AI Limits 会另外只保存最近一次经过清理的刷新状态。

观测值最多在本地保留 30 天，并且每个实例设有 1,024 条观测的安全上限。在该
上限内，最近 48 小时的观测按采集粒度保留；更早的留存历史只保留每个 UTC
小时内最后一条观测。断开实例或删除全部本地数据会删除对应 History；从外部
移除权限会保留 History，但在恢复权限或明确断开或删除前暂停刷新。

## 为什么图表提示还需要一次成功刷新？

所选配额窗口至少需要两条观测才能画出折线。升级时，AI Limits 可能会把当前的
有效快照按其原始获取时间保留为第一条观测。再成功刷新一次并且本次响应包含所选
配额窗口后，图表就会获得第二个点。

## 为什么图表中会出现空白或分开的线段？

当某次响应缺少该配额窗口、两条观测相隔超过 90 分钟，或重置边界发生变化时，
AI Limits 不会跨越这些位置连接折线。这样可以避免把服务商重置或刷新失败误画
成用量变化。当前响应中消失的配额窗口要等再次出现后才能选择。

## 切换 Used 和 Left 会改变保存的历史吗？

不会。AI Limits 只保存一个标准化的已用比例。**Used** 显示该比例，**Left**
显示其补数。这个偏好设置只改变显示方式，不会改变已保存的观测。

## 切换服务商账号前应该做什么？

请先在 AI Limits 中断开该实例，再切换账号。History 按服务实例和稳定指标标识
划分，并不使用持久化的服务商账号标识。断开连接会清除该实例的旧历史，避免把
同一套餐下两个账号的数据合并。

数据处理细节请参阅[隐私说明（英文）](PRIVACY.md)。如需报告问题，请使用
[GitHub Issues](https://github.com/TiantianFlow/ai-limits/issues)，并且不要提交 Cookie、
凭据、私人用量数据或其他机密信息。
